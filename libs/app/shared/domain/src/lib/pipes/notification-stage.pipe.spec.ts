import { NotificationStagePipe } from './notification-stage.pipe';

describe('NotificationStagePipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationStagePipe();
    expect(pipe).toBeTruthy();
  });
});
