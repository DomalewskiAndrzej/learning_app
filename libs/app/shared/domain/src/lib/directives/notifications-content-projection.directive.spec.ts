import { NotificationsContentProjectionDirective } from './notifications-content-projection.directive';

describe('NotificationsContentProjectionDirective', () => {
  it('should create an instance', () => {
    const directive = new NotificationsContentProjectionDirective();
    expect(directive).toBeTruthy();
  });
});
