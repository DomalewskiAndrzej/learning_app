import { CustomTemplateRefDirective } from './custom-template-ref.directive';

describe('CustomTemplateRefDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomTemplateRefDirective();
    expect(directive).toBeTruthy();
  });
});
